{
  "name": "mosaic-api",
  "version": "1.4.0",
  "description": "FFmpeg/Express.js video rendering api for the 'mosaic' web app",
  "main": "./dist/server.js",
  "scripts": {
    "start": "npm run build:live",
    "build": "tsc -p .",
    "build:live": "nodemon --watch 'lib/**/*.ts' --exec \"ts-node\" lib/server.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@ffmpeg-installer/ffmpeg": "^1.0.20",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "ffprobe": "^1.1.2",
    "ffprobe-static": "^3.0.0",
    "multer": "^1.4.4"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "nodemon": "^2.0.2",
    "ts-node": "^9.1.1",
    "tslint": "^6.1.3",
    "typescript": "^4.1.3"
  }
}
